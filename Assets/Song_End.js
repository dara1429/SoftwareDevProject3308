#pragma strict
//CURRENTLY NON-WORKING
//This file detects the end of the song and transitions to another screen.
function Start () {
	//GetComponent.<AudioSource>().Play();
	//sound();
}

function sound () {
	//yield WaitForSeconds (GetComponent.<AudioSource>().clip.length);
}
//Teleport to some menu after song finishes.
//Application.LoadLevel("MainMenu");